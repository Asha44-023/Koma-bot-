name: grass-bot-v31-1-market
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/cache@v4
        with:
          path: |
            cooldown.json
            active.json
          key: cooldown-v31-1-market
          restore-keys: |
            cooldown-v31-1-market

      - run: pip install requests

      - name: Clean expired + migrate
        run: |
          python3 -c "
          import json,time,os
          now=time.time()
          # FORCE CLEAR OLD CACHE ONCE - delete if signals older than 30min
          for fname, expire in [('cooldown.json', 1800), ('active.json', 7200)]:
              if not os.path.exists(fname):
                  open(fname,'w').write(json.dumps({'signals':{}} if 'cooldown' in fname else {}))
                  continue
              try:
                  d=json.load(open(fname))
                  if 'cooldown' in fname:
                      c={k:v for k,v in d.get('signals',{}).items() if now - v.get('t',0) < expire}
                      open(fname,'w').write(json.dumps({'signals':c}))
                      print(f'{fname} kept {len(c)}')
                  else:
                      d={k:v for k,v in d.items() if now - v.get('t',0) < expire}
                      open(fname,'w').write(json.dumps(d))
                      print(f'{fname} kept {list(d.keys())}')
              except Exception as e:
                  print(f'{fname} error {e} - reset')
                  open(fname,'w').write(json.dumps({'signals':{}} if 'cooldown' in fname else {}))
          "
          echo "=== COOLDOWN ==="; cat cooldown.json
          echo "=== ACTIVE ==="; cat active.json

      - env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python -u bot.py --once

      - run: |
          echo "=== FINAL ==="
          cat cooldown.json
          cat active.json
